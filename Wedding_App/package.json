{
  "name": "wedding-app-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix Wedding_App run dev",
    "build": "npm --prefix Wedding_App run build",
    "preview": "npm --prefix Wedding_App run preview",
    "install:frontend": "npm --prefix Wedding_App install"
  }
}